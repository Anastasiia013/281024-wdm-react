export const calcWeightIndex = (weight, height) => Number((weight / (height ** 2)).toFixed(2));

const PI = 3.1415926;

export const calcCircleSquare = raduis => raduis ** 2 * PI;

/*
export {
    calcWeightIndex,
    calcCircleSquare,
}
*/






