import styles from "./Footer.module.css";

const Footer = ()=> {
    return (
        <footer>
            <button className={styles.btn}>fotter button</button>
        </footer>
    )
}

export default Footer